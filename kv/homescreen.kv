#: import labels labels


<HomeScreen>:
    BoxLayout:
        orientation: "vertical"
        MDToolbar:
            title: labels.TITLE
            left_action_items: [["menu", lambda x: app.change_screen("settings_screen")]]

        Button:
            text: "Content"
            halign: "center"
